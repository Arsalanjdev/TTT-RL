# TicTacToe-RL

An reinforcement-learning project that learns to play perfect Tic-Tac-Toe through various RL algorithms (currently only value iteration of Q values).

---

## 🚀 Training

1. **Install dependencies**  
   ```bash
   pip install -r requirements.txt ```
   python q_value_iteration.py
